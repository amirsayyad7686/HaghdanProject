<%- include('partials/header') %>

<div class="relative w-full max-w-7xl mx-auto rounded-3xl overflow-hidden shadow-2xl mb-20 bg-slate-900 h-[600px] group">
    
    <div id="slider-container" class="flex h-full w-full transition-transform duration-700 ease-in-out">
        
        <div class="min-w-full h-full relative flex items-center px-6 sm:px-12 md:px-24">
            <div class="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1521737604893-d14cc237f11d?q=80&w=2000')] bg-cover bg-center opacity-20 mix-blend-overlay"></div>
            <div class="relative z-10 max-w-3xl">
                <span class="inline-flex items-center px-4 py-1.5 rounded-full text-[10px] sm:text-xs font-bold uppercase tracking-widest bg-emerald-500/20 text-emerald-400 border border-emerald-500/30 mb-6 backdrop-blur-sm">
                    <%= __('Participation & Creativity') %>
                </span>
                <h1 class="text-4xl sm:text-5xl md:text-7xl font-extrabold text-white tracking-tight leading-tight mb-6">
                    <%= __('Human Centric.') %><br><span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-teal-400"><%= __('Philanthropy & Empathy.') %></span>
                </h1>
                <p class="text-base sm:text-lg text-slate-300 max-w-xl mb-10 pr-4 rtl:pl-4 rtl:pr-0">
                    <%= __('slider_desc_1') %>
                </p>
                <div class="flex flex-col sm:flex-row gap-4">
                    <a href="/register" class="bg-emerald-600 hover:bg-emerald-500 text-white font-bold py-3.5 px-8 rounded-xl transition-all shadow-lg text-center"><%= __('Join the Syndicate') %></a>
                </div>
            </div>
        </div>

        <div class="min-w-full h-full relative flex items-center px-6 sm:px-12 md:px-24">
            <div class="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1542601906990-b4d3fb778b09?q=80&w=2000')] bg-cover bg-center opacity-30 mix-blend-overlay"></div>
            <div class="relative z-10 max-w-3xl">
                <span class="inline-flex items-center px-4 py-1.5 rounded-full text-[10px] sm:text-xs font-bold uppercase tracking-widest bg-blue-500/20 text-blue-400 border border-blue-500/30 mb-6 backdrop-blur-sm">
                    <%= __('True Needs vs Virtual Needs') %>
                </span>
                <h1 class="text-4xl sm:text-5xl md:text-7xl font-extrabold text-white tracking-tight leading-tight mb-6">
                    <%= __('Clean Water & Air.') %><br><span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-indigo-400"><%= __('Preserving Life.') %></span>
                </h1>
                <p class="text-base sm:text-lg text-slate-300 max-w-xl mb-10 pr-4 rtl:pl-4 rtl:pr-0">
                    <%= __('slider_desc_2') %>
                </p>
                <div class="flex flex-col sm:flex-row gap-4">
                    <a href="/projects" class="bg-blue-600 hover:bg-blue-500 text-white font-bold py-3.5 px-8 rounded-xl transition-all shadow-lg text-center"><%= __('View Pipelines') %></a>
                </div>
            </div>
        </div>

        <div class="min-w-full h-full relative flex items-center px-6 sm:px-12 md:px-24">
            <div class="absolute inset-0 bg-gradient-to-br from-slate-900 via-slate-800 to-emerald-900"></div>
            <div class="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1517048676732-d65bc937f952?q=80&w=2000')] bg-cover bg-center opacity-20 mix-blend-overlay"></div>
            <div class="relative z-10 max-w-3xl">
                <span class="inline-flex items-center px-4 py-1.5 rounded-full text-[10px] sm:text-xs font-bold uppercase tracking-widest bg-amber-500/20 text-amber-400 border border-amber-500/30 mb-6 backdrop-blur-sm">
                    <%= __('The Round Table') %>
                </span>
                <h1 class="text-4xl sm:text-5xl md:text-7xl font-extrabold text-white tracking-tight leading-tight mb-6">
                    <%= __('Replacing the Pyramid.') %><br><span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-400 to-orange-400"><%= __('Stewardship over Ownership.') %></span>
                </h1>
                <p class="text-base sm:text-lg text-slate-300 max-w-xl mb-10 pr-4 rtl:pl-4 rtl:pr-0">
                    <%= __('slider_desc_3') %>
                </p>
                <div class="flex flex-col sm:flex-row gap-4">
                    <a href="/cooperate" class="bg-slate-100 hover:bg-white text-slate-900 font-bold py-3.5 px-8 rounded-xl transition-all shadow-lg text-center"><%= __('Enter Matrix') %></a>
                </div>
            </div>
        </div>
    </div>

    <button id="prevBtn" class="hidden sm:block absolute left-4 rtl:left-auto rtl:right-4 top-1/2 -translate-y-1/2 bg-black/30 hover:bg-black/50 text-white p-3 rounded-full backdrop-blur-sm transition-all opacity-0 group-hover:opacity-100 cursor-pointer z-20">
        <svg class="w-6 h-6 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
    </button>
    <button id="nextBtn" class="hidden sm:block absolute right-4 rtl:right-auto rtl:left-4 top-1/2 -translate-y-1/2 bg-black/30 hover:bg-black/50 text-white p-3 rounded-full backdrop-blur-sm transition-all opacity-0 group-hover:opacity-100 cursor-pointer z-20">
        <svg class="w-6 h-6 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
    </button>
    
    <div class="absolute bottom-6 left-0 right-0 flex justify-center space-x-3 rtl:space-x-reverse z-20">
        <button class="dot w-3 h-3 rounded-full bg-white/50 hover:bg-white transition-colors cursor-pointer" data-index="0"></button>
        <button class="dot w-3 h-3 rounded-full bg-white/50 hover:bg-white transition-colors cursor-pointer" data-index="1"></button>
        <button class="dot w-3 h-3 rounded-full bg-white/50 hover:bg-white transition-colors cursor-pointer" data-index="2"></button>
    </div>
</div>

<div class="mb-24 px-4 sm:px-6 max-w-5xl mx-auto">
    <div class="text-center mb-10">
        <h2 class="text-3xl font-extrabold text-slate-900 tracking-tight"><%= __('Watch Our Vision') %></h2>
        <p class="text-slate-600 mt-3 text-lg"><%= __('video_subtitle') %></p>
    </div>
    
    <div class="relative w-full overflow-hidden rounded-3xl shadow-xl pt-[56.25%] bg-slate-900 border border-slate-200 group">
        
        <video 
            class="absolute inset-0 w-full h-full object-cover" 
            controls 
            preload="metadata"
            poster="/images/video-thumbnail.jpg" 
        >
            <source src="/videos/create_a_video_by_reading_thes.mp4" type="video/mp4">
            
            <%= __('Your browser does not support the video tag.') %>
        </video>

    </div>
</div>
<div class="mb-24 px-4 sm:px-6 max-w-6xl mx-auto">
    <div class="text-center mb-8">
        <h2 class="text-3xl font-extrabold text-slate-900 tracking-tight"><%= __('The Cooperation Tree') %></h2>
        <p class="text-slate-600 mt-3 text-lg"><%= __('A visual representation of our core philosophy and organic growth.') %></p>
    </div>
    
    <div class="md:hidden flex justify-center items-center gap-2 mb-4 text-emerald-600 font-bold text-xs animate-bounce">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4"></path></svg>
        <span><%= __('Swipe to explore or tap to zoom') %></span>
    </div>
    
    <div class="bg-white rounded-3xl shadow-xl border border-slate-200 p-2 sm:p-6 overflow-x-auto">
        <div class="min-w-[900px] md:min-w-full flex justify-center">
            <% if (lang === 'fa') { %>
                <img src="/images/tree-fa.png" alt="درخت همکاری" class="w-full max-w-5xl h-auto rounded-2xl cursor-pointer hover:opacity-80 transition-opacity" onclick="openLightbox(this.src)">
            <% } else { %>
                <img src="/images/tree-en.png" alt="The Cooperation Tree" class="w-full max-w-5xl h-auto rounded-2xl cursor-pointer hover:opacity-80 transition-opacity" onclick="openLightbox(this.src)">
            <% } %>
        </div>
    </div>
</div>

<div id="image-lightbox" class="fixed inset-0 z-[100] bg-slate-900/95 backdrop-blur-sm hidden flex-col opacity-0 transition-opacity duration-300" dir="ltr">
    
    <button onclick="closeLightbox()" class="absolute top-4 right-4 sm:top-6 sm:right-6 text-white bg-slate-800 hover:bg-red-500 rounded-full p-3 transition-colors cursor-pointer z-50 shadow-lg">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
    </button>
    
    <div class="w-full h-full overflow-auto overscroll-contain" onclick="closeLightbox()">
        <div class="flex items-center justify-center min-h-full w-max min-w-full p-4 pt-24 pb-12">
            
            <img id="lightbox-img" src="" class="max-w-none w-[1100px] md:max-w-full md:w-auto h-auto rounded-xl shadow-2xl cursor-default" onclick="event.stopPropagation()">
            
        </div>
    </div>
</div>

<script>
    function openLightbox(src) {
        const lightbox = document.getElementById('image-lightbox');
        const img = document.getElementById('lightbox-img');
        img.src = src;
        lightbox.classList.remove('hidden');
        lightbox.classList.add('flex');
        
        // Small delay to allow CSS fade-in animation
        setTimeout(() => lightbox.classList.remove('opacity-0'), 10);
        document.body.style.overflow = 'hidden'; // Locks background scrolling
    }

    function closeLightbox() {
        const lightbox = document.getElementById('image-lightbox');
        lightbox.classList.add('opacity-0');
        
        setTimeout(() => {
            lightbox.classList.add('hidden');
            lightbox.classList.remove('flex');
            document.body.style.overflow = 'auto'; // Restores background scrolling
        }, 300);
    }
</script>
<div class="mb-24 px-4 sm:px-6">
    <div class="text-center max-w-3xl mx-auto mb-16">
        <h2 class="text-sm font-black text-emerald-600 uppercase tracking-widest"><%= __('Our Core Manifesto') %></h2>
        <h3 class="text-3xl sm:text-4xl font-extrabold text-slate-900 tracking-tight mt-2"><%= __('Bringing the greatest good to the greatest number of people') %></h3>
        <p class="text-slate-600 mt-4 text-lg"><%= __('manifesto_subtitle') %></p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
            <div class="w-10 h-10 bg-emerald-100 text-emerald-600 rounded-xl flex items-center justify-center mb-4 font-bold">1</div>
            <h4 class="text-lg font-bold text-slate-900 mb-2"><%= __('Synergy & Empathy') %></h4>
            <p class="text-sm text-slate-600"><%= __('goal_1') %></p>
        </div>
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
            <div class="w-10 h-10 bg-blue-100 text-blue-600 rounded-xl flex items-center justify-center mb-4 font-bold">2</div>
            <h4 class="text-lg font-bold text-slate-900 mb-2"><%= __('Stewardship') %></h4>
            <p class="text-sm text-slate-600"><%= __('goal_2') %></p>
        </div>
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow bg-gradient-to-br from-white to-amber-50">
            <div class="w-10 h-10 bg-amber-100 text-amber-600 rounded-xl flex items-center justify-center mb-4 font-bold">3</div>
            <h4 class="text-lg font-bold text-slate-900 mb-2"><%= __('The Round Table') %></h4>
            <p class="text-sm text-slate-600"><%= __('goal_3') %></p>
        </div>
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow lg:col-span-2 bg-gradient-to-r from-slate-900 to-slate-800 text-white">
            <div class="w-10 h-10 bg-white/20 text-white rounded-xl flex items-center justify-center mb-4 font-bold">4</div>
            <h4 class="text-lg font-bold mb-2"><%= __('True Needs Priority') %></h4>
            <p class="text-sm text-slate-300"><%= __('goal_4') %></p>
        </div>
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
            <div class="w-10 h-10 bg-indigo-100 text-indigo-600 rounded-xl flex items-center justify-center mb-4 font-bold">5</div>
            <h4 class="text-lg font-bold text-slate-900 mb-2"><%= __('Transparent Fund') %></h4>
            <p class="text-sm text-slate-600"><%= __('goal_5') %></p>
        </div>
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
            <div class="w-10 h-10 bg-rose-100 text-rose-600 rounded-xl flex items-center justify-center mb-4 font-bold">6</div>
            <h4 class="text-lg font-bold text-slate-900 mb-2"><%= __('Collective Wisdom') %></h4>
            <p class="text-sm text-slate-600"><%= __('goal_6') %></p>
        </div>
        <div class="bg-white p-6 rounded-3xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow lg:col-span-2">
            <div class="w-10 h-10 bg-emerald-100 text-emerald-600 rounded-xl flex items-center justify-center mb-4 font-bold">7</div>
            <h4 class="text-lg font-bold text-slate-900 mb-2"><%= __('Money Cannot Buy True Needs') %></h4>
            <p class="text-sm text-slate-600"><%= __('goal_7') %></p>
        </div>
    </div>
</div>

<div class="bg-slate-50 rounded-[40px] p-8 sm:p-16 border border-slate-200 mb-20 relative overflow-hidden">
    <div class="absolute top-0 right-0 w-96 h-96 bg-emerald-500/5 rounded-full blur-3xl -translate-y-1/2 translate-x-1/3"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-blue-500/5 rounded-full blur-3xl translate-y-1/2 -translate-x-1/3"></div>

    <div class="text-center max-w-2xl mx-auto mb-16 relative z-10">
        <h2 class="text-3xl font-extrabold text-slate-900 tracking-tight"><%= __('The Ecosystem Lifecycle') %></h2>
        <p class="text-slate-500 mt-3 text-lg"><%= __('From isolated resources to executed industrial contracts.') %></p>
    </div>

    <div class="relative max-w-3xl mx-auto z-10">
        <div class="absolute left-[23px] sm:left-1/2 top-0 bottom-0 w-1 bg-slate-200 sm:-translate-x-1/2 rtl:left-auto rtl:right-[23px] sm:rtl:right-1/2 sm:rtl:translate-x-1/2"></div>

        <div class="relative flex items-center justify-between w-full mb-16 flex-col sm:flex-row">
            <div class="hidden sm:block w-5/12"></div>
            <div class="absolute left-0 sm:left-1/2 rtl:left-auto rtl:right-0 sm:rtl:right-1/2 w-12 h-12 rounded-full bg-slate-900 border-4 border-slate-50 flex items-center justify-center sm:-translate-x-1/2 sm:rtl:translate-x-1/2 shadow-md z-10">
                <span class="text-white font-bold text-sm">01</span>
            </div>
            <div class="w-full sm:w-5/12 pl-16 sm:pl-0 sm:pr-12 rtl:pl-0 rtl:pr-16 sm:rtl:pr-0 sm:rtl:pl-12 text-left sm:text-right rtl:text-right sm:rtl:text-left">
                <h3 class="text-xl font-bold text-slate-900 mb-2"><%= __('Workspace Registration') %></h3>
                <p class="text-sm text-slate-600 bg-white p-4 rounded-2xl border border-slate-200 shadow-sm inline-block w-full">
                    <%= __('Users identify their primary role (Provider vs. Builder) and gain access to their secure operational command center.') %>
                </p>
            </div>
        </div>

        <div class="relative flex items-center justify-between w-full mb-16 flex-col sm:flex-row-reverse">
            <div class="hidden sm:block w-5/12"></div>
            <div class="absolute left-0 sm:left-1/2 rtl:left-auto rtl:right-0 sm:rtl:right-1/2 w-12 h-12 rounded-full bg-emerald-600 border-4 border-slate-50 flex items-center justify-center sm:-translate-x-1/2 sm:rtl:translate-x-1/2 shadow-md z-10">
                <span class="text-white font-bold text-sm">02</span>
            </div>
            <div class="w-full sm:w-5/12 pl-16 sm:pl-0 sm:pl-12 rtl:pl-0 rtl:pr-16 sm:rtl:pr-0 sm:rtl:pr-12 text-left rtl:text-right">
                <h3 class="text-xl font-bold text-slate-900 mb-2"><%= __('Resource Underwriting') %></h3>
                <ul class="text-sm text-slate-600 bg-white p-4 rounded-2xl border border-slate-200 shadow-sm space-y-2 w-full">
                    <li class="flex items-start gap-2"><span class="text-emerald-500 mt-0.5">•</span> <%= __('Capital Ledgers: Liquid funds are registered openly.') %></li>
                    <li class="flex items-start gap-2"><span class="text-emerald-500 mt-0.5">•</span> <%= __('Facility Assets: Real estate and machinery are logged.') %></li>
                    <li class="flex items-start gap-2"><span class="text-emerald-500 mt-0.5">•</span> <%= __('Project Demands: Exact execution constraints are mapped.') %></li>
                </ul>
            </div>
        </div>

        <div class="relative flex items-center justify-between w-full mb-16 flex-col sm:flex-row">
            <div class="hidden sm:block w-5/12"></div>
            <div class="absolute left-0 sm:left-1/2 rtl:left-auto rtl:right-0 sm:rtl:right-1/2 w-12 h-12 rounded-full bg-blue-600 border-4 border-slate-50 flex items-center justify-center sm:-translate-x-1/2 sm:rtl:translate-x-1/2 shadow-md z-10">
                <span class="text-white font-bold text-sm">03</span>
            </div>
            <div class="w-full sm:w-5/12 pl-16 sm:pl-0 sm:pr-12 rtl:pl-0 rtl:pr-16 sm:rtl:pr-0 sm:rtl:pl-12 text-left sm:text-right rtl:text-right sm:rtl:text-left">
                <h3 class="text-xl font-bold text-slate-900 mb-2"><%= __('The Cooperation Matrix') %></h3>
                <p class="text-sm text-slate-600 bg-white p-4 rounded-2xl border border-slate-200 shadow-sm inline-block w-full">
                    <%= __('A live matchmaking environment where both parties cross-reference capabilities and initiate binding syndication requests directly to the counterparty\'s inbox.') %>
                </p>
            </div>
        </div>

        <div class="relative flex items-center justify-between w-full flex-col sm:flex-row-reverse">
            <div class="hidden sm:block w-5/12"></div>
            <div class="absolute left-0 sm:left-1/2 rtl:left-auto rtl:right-0 sm:rtl:right-1/2 w-12 h-12 rounded-full bg-indigo-600 border-4 border-slate-50 flex items-center justify-center sm:-translate-x-1/2 sm:rtl:translate-x-1/2 shadow-md z-10 shadow-indigo-500/50">
                <span class="text-white font-bold text-sm">04</span>
            </div>
            <div class="w-full sm:w-5/12 pl-16 sm:pl-0 sm:pl-12 rtl:pl-0 rtl:pr-16 sm:rtl:pr-0 sm:rtl:pr-12 text-left rtl:text-right">
                <h3 class="text-xl font-bold text-slate-900 mb-2"><%= __('Contract Execution') %></h3>
                <p class="text-sm text-slate-600 bg-white p-4 rounded-2xl border border-slate-200 shadow-sm inline-block w-full">
                    <%= __('Once a deal is accepted in the dashboard, direct communication lines open in the Contract Vault, bypassing the bottleneck and initiating physical execution.') %>
                </p>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const sliderContainer = document.getElementById('slider-container');
        const prevBtn = document.getElementById('prevBtn');
        const nextBtn = document.getElementById('nextBtn');
        const dots = document.querySelectorAll('.dot');
        
        let currentIndex = 0;
        const totalSlides = 3;
        let slideInterval;

        const updateSlider = () => {
            const isRTL = document.documentElement.getAttribute('dir') === 'rtl';
            const sign = isRTL ? 1 : -1;
            
            sliderContainer.style.transform = `translateX(${sign * currentIndex * 100}%)`;
            
            dots.forEach((dot, index) => {
                if(index === currentIndex) {
                    dot.classList.remove('bg-white/50');
                    dot.classList.add('bg-white', 'scale-125');
                } else {
                    dot.classList.add('bg-white/50');
                    dot.classList.remove('bg-white', 'scale-125');
                }
            });
        };

        const nextSlide = () => {
            currentIndex = (currentIndex + 1) % totalSlides;
            updateSlider();
        };

        const prevSlide = () => {
            currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
            updateSlider();
        };

        const startAutoPlay = () => {
            slideInterval = setInterval(nextSlide, 7000); 
        };

        const resetAutoPlay = () => {
            clearInterval(slideInterval);
            startAutoPlay();
        };

        nextBtn.addEventListener('click', () => { nextSlide(); resetAutoPlay(); });
        prevBtn.addEventListener('click', () => { prevSlide(); resetAutoPlay(); });

        dots.forEach(dot => {
            dot.addEventListener('click', (e) => {
                currentIndex = parseInt(e.target.getAttribute('data-index'));
                updateSlider();
                resetAutoPlay();
            });
        });

        updateSlider();
        startAutoPlay();
    });
</script>